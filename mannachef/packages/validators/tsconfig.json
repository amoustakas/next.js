{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "dist",
    "rootDir": ".",
    // `scripts/fuzz-schemas.ts` imports `../src/index.ts` by its real path so
    // `node --experimental-strip-types` can run it without a build step.
    "allowImportingTsExtensions": true
  },
  "include": ["src", "scripts"],
  "exclude": ["node_modules", "dist"]
}
